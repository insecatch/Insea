# This is a placeholder file since we can't directly create PNG files
# The app currently uses a gradient bug icon in the header which works well
# For a production logo, you would export a proper PNG file and place it here